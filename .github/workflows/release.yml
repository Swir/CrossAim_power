name: Build and Release CrossAim Power

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Generate app icon
        run: python make_icon.py

      - name: Syntax check
        run: python -m py_compile crossaim_power.py crossaim_power_v11.py crossaim_power_v12.py crossaim_power_v13.py crossaim_power_v131.py

      - name: Build EXE
        run: >
          pyinstaller --noconfirm --clean --windowed --onefile
          --name CrossAim_Power
          --icon crossaim_power.ico
          --add-data "crossaim_power.png;."
          --collect-all mss
          crossaim_power_v131.py

      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force release
          Copy-Item dist\CrossAim_Power.exe release\CrossAim_Power.exe
          Copy-Item README.md release\README.md
          Copy-Item LICENSE release\LICENSE
          Copy-Item RELEASE_NOTES.md release\RELEASE_NOTES.md
          Compress-Archive -Path release\* -DestinationPath CrossAim_Power_v1.3.1_Windows.zip -Force

      - name: Create or update v1.3.1 release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view v1.3.1 >/dev/null 2>&1; then
            gh release upload v1.3.1 \
              "release/CrossAim_Power.exe" \
              "CrossAim_Power_v1.3.1_Windows.zip" \
              --clobber
          else
            gh release create v1.3.1 \
              "release/CrossAim_Power.exe" \
              "CrossAim_Power_v1.3.1_Windows.zip" \
              --title "CrossAim Power v1.3.1" \
              --notes-file RELEASE_NOTES.md
          fi
